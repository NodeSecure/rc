{
  "type": "object",
  "properties": {
    "version": {
      "type": "string",
      "description": "version of the rc package used to generate the nodesecurerc file"
    },
    "i18n": {
      "type": "string",
      "enum": ["french", "english"],
      "default": "english",
      "description": "Language to use for i18n in NodeSecure tools"
    }
  },
  "required": ["version"],
  "additionalProperties": false
}
